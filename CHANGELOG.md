# 變更紀錄

本檔記錄各版本的修改與調整內容,標題版本號對應 GitHub Release tag。
`release.yml` 會依 tag 自動抽取對應段落,寫進 Release 說明與 `latest.json`(in-app 更新提示)。

## v0.13.3

本版把**手機分揀通道遙控頁**(工控機 `IP:18080/control`)全面改版,樣式與操作方式對齊 SortPause App。後端 API 不變,只需更新中介 PC。

### 調整
- **介面改版(淺色科技藍)**:由深色改為淺色玻璃擬態 + 科技藍配色 —— 白色卡片 + 左側藍色漸層強調條(暫停轉橙)、狀態膠囊、藍色物流晶片、細網格柔光背景,對齊 SortPause App 視覺。
- **操作改為主從導覽**:清單頁改成**點通道卡片進入單一通道詳情頁**(不再於清單直接切換開關,避免誤觸);詳情頁提供大型「暫停/啟用」按鈕、跳過本輪、當前物流單號(後四碼放大)與最近歷史;右上鈴鐺進入雲端查件異常清單(未讀紅點)。通道位置顯示改「左1/右1」,語言鈕改「中·VI」單鍵切換,支援手機返回手勢/返回鍵。

### 修正
- **跳過操作失敗會還原**:跳過 / 取消跳過送出失敗時,還原樂觀顯示的待跳過次數,避免畫面數字與失敗提示矛盾。
- **異常頁鈴鐺不誤亮**:停留在異常清單頁時,輪詢新到的警示即時標記為已讀,離開後鈴鐺未讀數不再誤亮。
- **空單號正確顯示**:詳情頁當前單號為空時顯示「—」,不再出現字面「null」。
- **未設定通道顯示狀態**:未設定通道代碼的通道也顯示「分揀中 / 已暫停」狀態膠囊。

## v0.13.2

本版接續 v0.13.1,讓面單預產的失敗單號清單**一眼就看得到**。

### 調整
- **手動預產失敗清單移到結果區最上方**:失敗清單原本在頁面最底部,大量預產時被上萬張縮圖埋住、要滑很久才看得到;現在移到統計列正下方、縮圖之前,有失敗時第一眼就看到。紅色描邊醒目、表格限高捲動,「複製單號 / 重跑失敗單號」按鈕同在最上方。
- **自動排程失敗清單預設展開**:排程狀態卡的失敗單號原本收合需點一下才展開,現在預設直接列出(仍可手動收合)。

## v0.13.1

本版強化面單預產的失敗處理與印單統計,並把面單預產去重的「換日」時間點調整為 **06:00**。

### 新功能
- **面單預產:失敗單號看得到、可複製、可一鍵重跑**
  - 手動預產(訂單/袋號/日期):失敗清單新增「複製單號」與「重跑失敗單號」按鈕。**重跑/複製涵蓋全部失敗單號**,不受畫面表格最多顯示 300 筆的限制。
  - 自動排程:上次執行的失敗單號會保留(最多 200 筆),在排程狀態卡可展開列出、複製、一鍵重跑 —— 修正前自動排程只記「失敗 N 筆」,看不到是哪些單號、無法針對性重跑。
- **印單統計:新增區間「讀碼失敗(NoRead)」件數**:依所選日期區間顯示工控機相機讀不到單號的件數,與區間總數以同一時區基準計算、日界對齊。

### 行為調整
- **面單預產去重換日改為 06:00**:面單預產「今日已預產」的判定範圍,換日時間由原本的 04:00 調整為 **06:00**(業務日 06:00~次日 05:59),更貼合現場夜班作息。不影響印單統計與快取檔清理的既有邏輯。

### 修正
- **NoRead 統計日界對齊**:印單統計的 NoRead 件數改由逐筆查詢紀錄(本地時間)計數,與區間總數同基準 —— 修正前以 UTC 聚合資料比對本地日期,清晨的 NoRead 會被算到前一天、與區間總數對不上。
- **重跑失敗單號涵蓋全部**:失敗數超過畫面顯示上限(300 筆)時,「重跑失敗單號」不再只重試前 300 筆而靜默漏掉其餘。

## v0.13.0

本版為全面品質強化版:經四輪深度程式碼審查,修正 48 項正確性、競態與靜默失敗問題。**建議所有站點更新**,特別是使用直接列印(direct_print)模式或快取目錄留空(預設)的站點。

### 重要修正(現場可感知)
- **直接列印失敗不再靜默**:direct_print 模式下面單下載失敗、物流商未設印表機、讀圖或列印失敗時,桌面會跳常駐錯誤提示 + 警示音並記入事件記錄 —— 修正前工控機收到成功回應、統計已記,現場卻整批漏印且毫無提示。
- **未指派通道不再誤印/誤計**:物流商未指派分揀通道時,四種面單模式行為一致(不印、不下載、不計印單統計、不標袋核對已印),事件記錄會提示到「指派物流」頁補設定 —— 修正前 direct_print 會印出一疊無格口可分揀的面單,統計卻顯示全部正常。
- **本機服務未啟動主動告警**:連接埠被占用導致服務沒起來(啟動時或改設定重啟時),畫面會跳常駐紅色警示 + 警示音 —— 修正前只有 log,工控機整線連不上卻沒有任何提示。
- **快取目錄安全防護**:禁止把快取目錄設成系統資料夾(圖片/文件/下載/桌面等)或磁碟根目錄 —— 修正前預設設定下「清空快取」與每小時自動清理會把使用者「圖片」資料夾內的個人檔案當過期快取刪除(不可回復)。多層防護:預設改用專屬子資料夾、設定頁擋下危險目錄、清理器只清有識別標記的目錄。
- **跨機同步更可靠**:WebSocket 斷線偵測(主動心跳 + 閒置逾時,網路切換/睡醒後不再靜默失聯)、重連退避正常化(閃斷快速重連、雲端故障時不打連線風暴)、重連後清關進度框自動重拉基準補回斷線期間遺失的廣播。
- **清關進度框數字不再凍結**:大區間(雲端裁明細)模式下改為事件觸發自動重拉,剩餘數字不再停住或跳成錯誤的極小值。
- **面單預產排程不漏時段**:批次執行時間過長跨過下一時段時,恢復後自動補跑錯過的時段;跨午夜的邊界(避免錯日預產)會明確記入事件記錄,不再靜默消失。
- **錯誤面單單一出口**:掃描列印/自動印單遇雲端業務錯誤(4xx)時,錯誤面單一律由前端用該物流商設定的同一台印表機印出,並計入失敗統計 —— 修正前三條路徑行為不一(有的印錯台、有的完全不印、統計缺漏);錯誤面單印不出來(無印表機/列印失敗)也會提示。
- **失敗訊息更清楚**:掃描列印/預產頁的失敗項目優先顯示雲端回覆的原因說明,不再只有狀態碼。

### 其他修正
- 改設定更安全:改快取目錄即時生效(/images 同步切換,不再 404);改無關設定不再重啟相機(存證不漏拍)或重連同步;壞的快取目錄設定不再卡死其他設定的儲存。
- 「允許無效憑證」開關現在對面單下載同樣即時生效(修正前僅 API 生效,自簽環境面單全數下載失敗);面單下載逾時保底 30 秒,不受 API 逾時調低影響。
- 掃描列印/入倉箱標列印不再阻塞背景服務(Windows spooler 卡住時工控機查件不受影響)。
- 手機遙控頁面對條碼/人員名稱做 HTML 跳脫,雜訊條碼不再弄亂版面(消除注入風險)。
- 自動印單:人員未填時掃包裹條碼仍會載入清單(只擋列印),補完資料後立即重掃同一條碼不再被誤判重複。
- 設備異常廣播去抖回歸「同類型 20 秒一次」語意,工控機夾帶變動訊息時不再打斷語音、洗版提示。
- 儀表板成功率分母排除讀碼失敗(NoRead),更準確反映雲端出單成功比例。

### 升級注意
- **快取目錄留空(預設)的站點**:快取根由「圖片」資料夾改為其專屬子資料夾(`圖片/cix3752iLabelPrint`),升級後首日面單會重新下載(自動完成,無需操作);原「圖片」資料夾內的舊面單圖不會被自動清除,可自行整理。
- 若曾將快取目錄手動設為系統資料夾或磁碟根目錄,升級後會自動改用安全位置並記入事件記錄,請至快取設定頁改設專屬資料夾。

## v0.12.0

本版新增「讀碼失敗(NoRead)處理」與「袋件連續性異常偵測」,並優化統計準確度與現場提示。

### 新功能
- **讀碼失敗(NoRead)處理**:工控機相機讀不到單號時,以 `NoRead` 呼叫即可 —— 系統不再誤把它當單號去查雲端(避免一律「查無訂單」的噪音與多餘的錯誤面單),但**仍會拍照存證**(檔名含時間與序號,同一秒多筆也不互相覆蓋),並計入當日「讀碼失敗」件數。桌面只跳提示訊息、不出聲(讀碼失敗屬高頻,不宜狂響);回應明確標示 `NOREAD`,工控機無需回報,依現場異常流程處理即可。
- **袋件連續性異常偵測**:件數核對頁的每個袋,若「還沒印完就中途出現別的袋號」,會在卡片標紅「中途被打斷」提醒現場注意。讀碼失敗 / 散單不會誤判為打斷;回頭把該袋補齊後自動恢復正常;回頭補印「已完成的袋」也不會誤報。

### 調整
- **今日成功率不再被讀碼失敗拉低**:成功率分母排除 NoRead(讀碼失敗未送雲端,不算雲端查詢失敗),更準確反映雲端 / 出單的實際成功比例;儀表板「本日請求數」旁另顯示今日讀碼失敗件數。
- 讀碼失敗提示改用在地化文案(繁中 / 越南語);高頻讀碼失敗時提示會自動合併,不再洗版蓋掉「未登入 / 門市關轉」等真正需要處理的告警。
- 讀碼失敗存證照片改為背景寫檔,不再讓工控機多等磁碟寫入即可拿到回應。

### 文件
- 更新 `README.md` / `CLAUDE.md`:補齊件數核對、清關作業、入倉驗單、讀碼站存證、手機遙控通道等既有功能說明,修正設備異常廣播的過時描述(已改固定廣播一次 + 去抖),面單路徑補上 `direct_print` 模式。工控機 API 文件 `docs/local-http-api.md` 補上 NoRead 契約。

## v0.11.0

本版新增「提示音可自訂」,並優化桌面視窗開啟行為。

### 新功能
- **提示音可自訂**:自動印單頁與雲端 API 頁可自訂「列印成功 / 查無或失敗 / 門市關轉 / 未確認」四種提示音;入倉驗單頁可自訂「錯誤 / 重複入箱 / 特殊包裹」提示音。內建 118 種音效可選,附試聽與「恢復預設」,選擇存於各台機器本機(各機獨立記憶,重開仍在)。自動印單與雲端 API 共用同一組全域提示音,改一處兩頁同步。
- **桌面視窗開啟即最大化**:App 啟動自動最大化填滿螢幕,並限制視窗最小尺寸,避免縮太小導致版面錯位。

### 調整
- 所有內建提示音統一為 mp3 格式,確保 Windows / macOS / Linux 各平台皆能正常播放(原部分音效在特定 Linux 版可能無聲)。
- 提示音設定改用內建音效庫驗證:若選過的音效在日後版本被移除,會自動退回預設,不再出現「設定存在卻沒聲音」的靜默狀況。
- 修正入倉驗單頁誤將「入箱成功」列為可自訂(該狀態一律播箱號語音、beep 不會生效),避免設定了卻無效果的困惑。
- 試聽音效在關閉設定視窗時會立即停止,不再繼續播完。

## v0.10.1

本版為 v0.10.0 的品質修正,針對「入倉驗單 / 設備異常廣播 / 面單預產」做多項健壯性強化,消除數個會「靜默失敗、讓現場誤以為正常」的狀況。

### 修正
- **入倉驗單不再靜默漏件**:掃描 / 建箱 / 移除遇到未預期的回應時,一律會發出錯誤提示音與訊息,不再無聲無息讓人以為成功。入箱語音報號改用送出當下的箱號,連續刷碼期間不會報錯箱號。
- **設備異常廣播更精準**:20 秒去抖改以「異常類型 + 內容」判斷——持續狂丟的同一則訊號照樣只提示一次,但同類型的**不同故障**(例如不同通道、不同印表機)不會被吞掉,仍會提示。
- **面單預產去重更可靠**:
  - 夜班跨午夜(00:00–04:00)的預產記錄不再於凌晨被清掉而重跑、誤報成功。
  - 資料庫短暫讀取失敗時不再把「已預產」誤判為空、導致整批重跑。
  - 大批標記改批次寫入,預產頁不再因大量訂單而卡頓。
  - 「清除已預產記錄」若後端未清成功,會明確提示而非假性清空。
- **箱標列印防呆**:箱標資料缺關鍵欄位時中止並提示,不再印出空白 / 殘缺箱標卻回報成功。
- **音效更穩定**:強化跨平台音訊解鎖與音檔缺失偵測,降低現場聽不到提示音的機會。

## v0.10.0

本版新增「入倉驗單」作業頁,並優化設備異常廣播與面單預產的去重行為。

### 新功能
- **入倉驗單**:掃描包裹條碼快速入倉建箱,支援倉庫 / 物流商 / 日期 / 箱號設定、二段條碼、大件標記,並可即時看到已入清單與件數。**箱標直接走本機印表機列印**(不需雲端出圖),支援連續箱號批次列印。入箱成功會以**預錄人聲報出箱號**(「入第 N 箱」),倉庫現場吵雜環境也聽得清楚。

### 優化
- **設備異常語音廣播**:移除「重複次數」設定,改為每次固定廣播一次;並新增**同類異常 20 秒去抖**——工控機對持續性異常(如卡包裹)狂丟訊號時,不再造成語音互相打斷、聽不清或提示洗版,20 秒內同一種異常只提示一次。
- **面單預產去重統一**:自動排程與手動執行現在**共用同一份「今日已預產」記錄**。自動排程跑完後再手動執行,已預產的訂單會正確**略過**、不再重複請求雲端、不再一堆「成功」。記錄存本機資料庫,App 重啟後仍記得。
- **介面**:清關(包裹入廠 / 司機派工)與入倉驗單各作業卡片標題統一加上底色;數字輸入改用內建加減步進元件;輸入欄位標題排版一致化。

### 注意
- 「入倉驗單」需搭配雲端對應更新上線後才能使用。雲端更新上線前,點開該頁反查會顯示查詢錯誤;其餘功能不受影響。

## v0.9.0

本版新增兩項跨機協作功能,讓多台分揀工控機與操作端能即時共享列印進度。

### 新功能
- **件數核對跨機同步**:同一袋包裹被分到別台機器處理時,各台的件數核對清單也能即時同步。透過雲端 WebSocket(Reverb)廣播「某件已印」事件,各中介端訂閱自己持有的袋,收到後就地更新——不再因「舊袋不再回本機」而失真。重連 / 新訂閱時自動向雲端補回斷線期間漏掉的列印(雲端為真相來源)。
- **清關進度浮動框**:指定報關日(可區間)即時掌握當日清關進度——袋數(剩/總)、件數(剩/總)。全域常駐浮動框,跨頁不消失、可拖曳、記住位置,只有手動關閉才收起。隨各機台列印即時遞減剩餘;報關日事後補上 / 改動也會即時反映,全程不需手動刷新。
- **跨機同步設定**:伺服器設定頁新增「跨機同步」區塊,可開關同步並設定 Reverb 連線(host / port / scheme / app key),即時熱套用、無須重啟。

### 注意
- 清關進度浮動框需搭配雲端對應更新才會顯示真值與即時變化。雲端更新上線前,點開浮動框會顯示查詢錯誤;件數核對跨機同步不受影響、可正常運作。

## v0.8.1

本版修正面單預產「清不掉快取、無法重跑」的問題,並補上強制重跑的操作入口。

### 修正
- **面單預產無法重跑**:預產有兩層各自獨立的去重(前端記「本快取日已預產過的訂單」+ 後端比對快取來源 URL),原本清空後端快取也碰不到前端那層,導致已預產過的訂單一律被標「略過」、無法重新抓取。現在三條路徑任一都能真正重跑。

### 新功能
- **面單預產「強制重跑」開關**:開啟後忽略「已預產」記錄,並要求後端重新向雲端下載、覆蓋既有快取面單(排除快取檔陳舊 / 毀損,或雲端面單已更新的情況)。
- **「清除已預產記錄」鈕**:一鍵清掉本快取日的已預產記憶,之後重新查詢即全部重抓;有記錄時才顯示並標示筆數。
- **清空快取連帶清預產記錄**:在「快取設定」頁清空快取時,會一併清掉面單預產的前端去重記憶,讓「清快取」符合直覺地能真正重跑。

## v0.8.0

本版新增「讀碼站快照存證」:收到工控機查件請求時,後端背景拍下讀碼站當下畫面存檔,供「沒貨卻出紙」爭議時調閱佐證。並接入重複啟動防呆,根治「重複開啟 App 搶不到埠導致反覆崩潰」。

> 相機功能預設關閉,需在「快取設定」頁開啟並選對相機裝置才會運作;未啟用 / 未接相機完全不影響既有出單流程。

### 新功能
- **讀碼站 USB 相機快照存證**:收到工控機 `GET /api/parcel` 當下,後端抓 USB 相機最新一幀存檔,並把路徑寫進該筆請求記錄(請求記錄頁可直接調出當下畫面)。用途:分揀線出現「沒貨卻出紙」爭議時,有讀碼站此刻的實際畫面可佐證有無實體包裹。相機常駐取流、請求進來毫秒級複製當下幀,完全不拖慢工控機回應;相機未啟用 / 未接 / 沒權限時自動略過(該筆無快照),不影響正常出單。
- **相機設定與即時預覽**:「快取設定」頁可開關相機、選裝置索引、調 JPEG 品質與**數位變焦**(USB 相機多半無光學變焦,以裁切畫面中央達成「拉近」框景,讓相機擺位受限時仍能對準讀碼站),並可手動拍一張測試存檔。切換開關 / 換鏡頭 / 調變焦皆即時生效,無須重啟。
- **存證獨立保留**:快照存放目錄與保留天數(預設 90 天,0=永久)與面單快取完全分離、各自獨立清理,避免爭議佐證被面單快取的清理週期提早刪除。

### 調整與健壯性
- **重複啟動防呆**:接入 single-instance,重複開啟 App 時只把既有視窗帶到前景,不再因第二個實例搶不到 18080 埠而整個崩潰。
- **伺服器埠被占用不再致命**:啟動時若 18080 被占,App 仍正常開啟、狀態顯示「server 未啟動」,排除占用後可從設定頁手動重啟,不再閃退。
- **同埠重啟修正**:只改存證目錄等設定、埠沒變時的重啟改為「先釋放舊埠再綁新的」,解決重啟撞「位址已被使用」而失敗。

## v0.7.0

本版新增「清關作業」功能群組(包裹入廠 / 司機派工),操作員可掃袋號批次建檔與派工;並重整側邊欄分類順序。

> 需搭配雲端後端對應的清關 API(`clearance/options`、`clearance/store`、`clearance/dispatch`)一併部署才會生效。

### 新功能
- **清關作業 — 包裹入廠**:掃描(或貼上多筆)包裹袋號,選清關公司、清關日期、倉庫後一次建檔到雲端;清關日為當天時自動觸發訂單確認。清關公司下拉由雲端帶回歷史清單、亦可自由輸入新值;選過的清關公司與倉庫會本地記憶,切換頁面或重開 App 都不需重選。
- **清關作業 — 司機派工**:掃描袋號批次指派出清司機與出清日期;司機名單同樣由雲端帶回 + 可自由輸入 + 本地記憶(倉庫與包裹入廠頁共用同一記憶)。

### 調整
- 側邊欄重整:新增「清關作業」群組(置於「訂單列印」之下);「面單預產」移至「儀表板」下方;「印表機設定」由「訂單列印」移至「設定 → 指派物流」下方。
- 掃描批量輸入元件對外開放聚焦方法,清關兩頁載入 / 清空後游標自動回到掃描欄。

## v0.6.2

本版新增掃描槍連刷防呆,並讓 macOS 安裝包正式簽章公證(別台 Mac 下載即開)。

### 新功能
- **掃描槍連刷防呆**:同一條碼在 5 秒內被重複刷入會自動略過,避免掃描槍「刷太快出兩筆」造成同一筆重複列印。自動列印頁的袋號欄與訂單編號欄各自獨立判定(兩種掃描意圖不互相干擾);被擋下的重複僅以靜默 toast 提示、不播音效(現場連刷頻繁,每筆都響會很吵)。時間錨定在「首次有效掃描」,真正隔一段時間(>5 秒)的重印仍可正常通過。

### 調整
- **macOS 安裝包正式簽章 + 公證(notarize)**:發版 CI 接上 Apple Developer ID 憑證,產出的 `.app` / `.dmg` 通過 Gatekeeper,別台 Mac 下載後可直接開啟,不再出現「已損毀,無法打開」。未設定簽章 secret 時自動退回 ad-hoc,不影響建置。

## v0.6.1

本版為 v0.6.0 的修正與健壯性強化(無新功能,建議升級)。

### 修正
- **快取容量上限清理失效**:容量上限淘汰原以「掃磁碟總量」為目標,但磁碟另含浮水印 / 錯誤面單等非受管孤兒檔,扣不到目標會把合法快取整批刪光、容量卻仍超標。改以受管快取(`cache_meta`)加總為基準。
- **推送佇列殭屍項**:App 中途關閉 / 崩潰時卡在 `sending` 的回報項會永久卡死不再推送。新增「啟動回收」+「卡住逾 60 秒在席回收」雙重兜底,確保任何路徑下的 `sending` 都能救回。
- **推送佇列改 capped 指數退避**:失敗重試由「固定每 5 秒硬重試」改為指數退避(5s→10s→…→上限 300s);達重試上限改以 error 級事件 + `last_error` 落庫留痕,不再靜默丟件。
- **分揀通道「跳過本輪」並發競態**:round-robin 的 skip 額度消耗改為持鎖跨 DB 原子扣減(條件 UPDATE),杜絕高頻分揀時的 double-skip(同一次跳過被算兩次)。
- **伺服器監聽 IP / 埠變更不生效**:設定頁改 `listen_ip` / `port` 後僅存檔卻沒重綁 socket(設定存了卻沒生效)。改為變更時自動重啟 server;新位址綁定失敗則整筆設定中止、不持久化,避免斷鏈。
- **Windows 列印失敗送出空白頁**:影像繪製失敗時改 `AbortDoc` 丟棄整份文件,不再把半張 / 空白頁 commit 進列印佇列。
- **面單預產假性成功**:預產走 UI 路徑時,下載失敗會退回原雲端 URL 而被誤判為「已快取」。改為直接下載到快取並傳播真實成敗,已快取者免重抓。
- **回報佇列清除時區偏差**:`queue_purge` 比對基準補 `localtime`,修正與本地時間寫入差 8 小時導致清除範圍錯誤。
- **物流商刪除半完成**:刪除物流商與解除通道指派改為同一交易,中途失敗整批回滾,避免「指派關聯刪了、物流商還在」。

### 調整
- **查件異常記錄補物流單號**:雲端業務錯誤(400)帶出的精確物流單號(`shipping_no`)寫入查件異常記錄;異常清單新增「物流單號」欄,通道最近活動優先以物流單號對單(與工控機掃的查詢條碼區分)。
- **查件異常類別名稱單一走 i18n**:移除前端寫死的中文常數(易與雙語 i18n 漂移、曾致類別語意顯示錯誤),統一由 `parcelAlert.*` 提供中越雙語。
- **雲端查件連線層重試**:工控機同步熱路徑只重試連線層瞬時錯誤(連線被拒 / 重置),不重試 timeout,避免雲端 hang 時放大工控機等待。
- **多處 DB 讀取與錯誤面單編碼留痕**:原本靜默吞錯的位置改記錄 warn / error 事件,不再讓表面正常掩蓋問題。

## v0.6.0

### 新增
- **手機遙控分揀通道**:同區網手機開 `http://{本機IP}:18080/control` 即可暫停 / 恢復任一通道、跳過本輪(換紙、卡關時免回桌機操作)。控制頁自帶 CSS/JS、離線可用;桌面與手機任一端切換即時互相同步(後端廣播 `sort-channel-updated`)。桌面端在頂部導覽列新增遙控入口(QR + 本機 `IP:port` 連線位址,可一鍵複製填入)。
- **多螢幕看板子視窗**:件數核對 / 列印統計可開成獨立子視窗,分配到第二螢幕當現場看板。
- **雲端查件異常記錄**:`GET /api/parcel` 雲端回業務錯誤(門市關轉 / 未確認 / 找不到 / 非代收代寄 …)時寫入記錄,新增「查件異常」記錄頁回看(可看到原始雲端 code、查詢條碼、解析到的分揀通道)。手機控制頁與桌面皆可查閱最近異常。
- **分揀通道「跳過本輪」**:某物流 1 對多 round-robin 輪流分配時,若某通道臨時卡關,可累加跳過次數;輪到該通道時消耗一次並改派下一個通道,不必整列暫停。

### 調整
- **分揀通道指派物流改 1 對多**:單一通道可指派多個物流代碼;通道卡片新增快速暫停 / 啟用開關,分揀進行中即時生效(獨立於整列儲存)。
- **面單預產自動排程**:強化觀測性(排程執行記錄)與開機補跑(關機錯過的時段開機後自動補抓)。
- **手機遙控入口移至導覽列**:原分揀通道頁標題的「手機遙控」按鈕移到頂部導覽列(全頁可用),連線位址改顯示純 `IP:port` 並加複製按鈕,方便填入只能輸入 IP 的設備 App;QR 仍編完整網址供手機瀏覽器開遙控頁。

## v0.5.8

### 新增
- **印單統計新增「依分揀通道」**:依工控機分揀機出單的分揀通道代碼,統計區間內各通道去重後的印單數。為此 `print_event` 新增 `channel_code` 欄位(僅 `ipc` 工控機來源帶值),升級後開始累積。
- **面單預產自動排程**:新增常駐背景排程,每天到指定時間點(可自訂多個)自動反查當日整批訂單並預下載面單到本機快取——免開預產頁、免人操作(headless)。可選清關日期 / 轉寄出貨來源;每日去重(多時段重跑只抓新出現的單);預設關閉,於「面單預產」頁的「自動排程」設定。

### 調整
- **件數核對頁改版**:
  - 卡片改 Masonry 瀑布流(最新在左上、由左到右填滿,不等高也向上緊貼補空隙,重排平滑滑動)。
  - 訂單明細改為可收合(預設收合,點「展開」開啟)。
  - 列表精簡:移除序號欄、簡碼移至最前、列印時間只顯示 `HH:MM:SS`、欄寬縮窄;移除與下方統計重複的「未印 N 件」徽章。
  - **保留邏輯調整**:有未印件的袋持續保留、永不淘汰(現場可隨時回補列印);已完成的袋只保留最新一袋(原為固定最近 3 袋)。
  - 移除「整袋清單載入失敗」狀態:散單 / 未登入 / 雲端查詢失敗時不再建立錯誤卡(當下那件本身已正常列印,僅次要的整袋交叉核對取不到清單),失敗改記事件記錄。

### 開發
- 修正 `tauri.conf.json` 的 `devUrl`(`1420` → `11420`)對齊 `vite.config.js` 的 dev port,`npm run tauri:dev` 才連得上前端(僅影響開發,不影響打包產物)。

## v0.5.7

### 修正
- 「請求記錄」頁輸入關鍵字必出現 `資料庫錯誤: (code: 20) datatype mismatch`：SQL 以 `?1` 編號參數混用匿名 `?`（LIMIT/OFFSET），sqlx 位置綁定與 SQLite 參數編號規則對不上，LIKE 字串被綁進 LIMIT。改為全匿名 `?` 重複綁定，並新增回歸測試（`src-tauri/tests/parcel_query_log_keyword.rs`）防再犯。

### 新增
- 「請求記錄」進階查詢新增**耗時篩選**：可選欄位（任一 / 雲端 / 圖案 / 總花）搭配毫秒門檻，列出超過執行時間的查詢，便於定位慢請求；可與關鍵字疊加。

### 調整
- 錯誤面單版面：移除整張外框線；新增列印日期 `MM/DD`（非粗體，位於分隔線與圖示之間置中，DejaVu Sans Regular 編譯期內嵌、無須部署字型檔）；`UNCONFIRMED` 問號圖示上移對齊視覺中心。

## v0.5.6

### 修正
- 錯誤面單回應 `channel_code` 一律為 `null`，分揀機無格口可分揀導致不列印：現改為照**正常面單同一套流程**解析分揀通道——雲端錯誤帶出 `shipping_provider` 時（門市關轉 / 未確認 / 狀態異常 / 非代寄 / 非轉寄 / 出單失敗），走指派通道 round-robin 與 `print_profile` 查詢；查不到物流商時（查無訂單 / 雲端連線失敗）統一退回「未指派通道代碼」。只要設定頁有設未指派通道，**所有錯誤面單都保證有 `channel_code`**。
- `direct_print` 模式錯誤面單印表機選擇：可判斷物流商時優先用該物流商在「指派物流」頁設定的印表機（原為無序取任一台已設定印表機，可能印到別站）。

- 錯誤面單回報 `POST /api/report` 會失敗：錯誤查詢先前不寫入查詢記錄且 `response_id` 為 `null`，工控機照正常流程回報時反查不到記錄而收到 422。現錯誤查詢**同步寫入查詢記錄**（`response_id` 用本地負數，與雲端正數 ID 區隔），回報可正常配對回 200（只記錄本機、不推雲端 webhook）；「查詢記錄」頁同時也看得到錯誤查詢（含耗時欄位），不再是診斷盲區。
- 「事件記錄」與「佇列歷史」頁的關鍵字搜尋無作用：過濾邏輯只實作在瀏覽器預覽的 mock 分支，實際 App 內關鍵字從未送到後端。現後端 `event_log_list`（對 `message` / `action`）與 `queue_list`（對 `tracking_no` / `sort_channel` / `job_sticker`）支援 LIKE 模糊查詢，前端同步帶入關鍵字。

### 工控機整合注意
- 錯誤面單（`is_error_label: true`）回應的 `channel_code` / `print_profile` **不再一律為 `null`**，且帶**負數 `response_id`**：工控機把錯誤包裹當一般面單跑完整流程（分揀進 `channel_code` 通道 → 列印 `label_path` → `POST /api/report` 回報）即可，**端上零特殊處理**。詳見 `docs/local-http-api.md`。
- 雲端 API（cix3752iWeb）需同步部署：`OrderPrintController::failResp` 在查得到訂單的業務錯誤帶出 `shipping_provider`。雲端未部署時，所有錯誤面單退回未指派通道代碼（不影響運作，但無法分到物流商對應通道）。

## v0.5.5

### 新增
- **設備異常語音廣播 API（`POST /api/device-alert`）**：工控機回報設備異常（分揀機台卡包裹、USB 接口異常斷線、掃描器 / 印表機故障等）時，桌面 App 以**中文 + 越南語雙語語音**廣播喊話現場人員，並顯示 toast 提示。語音採**預錄音檔內嵌**（中文 `HsiaoChen`、越南語 `HoaiMy` neural 音色），每台機器音色一致、發音標準、**離線可用、越南語免在 Windows 端裝語音包**；僅未錄音的自訂 `type` 才退回系統 TTS。支援 `repeat` 控制廣播次數（預設 1、上限 3）、`message` 補充細節（顯示於 toast）。內建分類碼：`PARCEL_JAM` / `USB_DISCONNECT` / `SCANNER_ERROR` / `PRINTER_ERROR` / `ERROR`。
- **面單預產去重快取**：面單預產頁在同一「快取日」（以 04:00 為界，對齊面單快取清理週期）內，已成功預產過的訂單編號不再重打雲端 `cloudFetchLabel`，避免大量批次重複請求、降低伺服器負擔。統計列新增「略過」筆數顯示，縮圖以綠色勾選圖示標示被略過的訂單。

### 工控機整合注意
- 新增 `POST /api/device-alert` 端點：**立即回 200**（不讓工控機等），語音廣播由桌面 App 背景處理。`type` 自動轉大寫（大小寫皆可傳），省略時當作 `ERROR`；`repeat` 自動限制在 1～3。此端點與查件 / 回報流程無關，可獨立呼叫。詳見 `docs/device-alert-api.md`（亦提供 `.docx` 給整合廠商）。

## v0.5.4

### 修正
- 錯誤面單在 `http` / `local` / `share` 面單路徑模式下印不出來：根因為錯誤面單只走中介機本機印表機（`find_any_printer`），但這些模式下印表機在工控機端、中介機無印表機，導致錯誤面單被靜默丟棄（先前僅 `direct_print` 模式可用）。修正後工控機 `GET /api/parcel` 遇雲端業務錯誤改回 **HTTP 200** + `is_error_label` 旗標與 `label_path`，錯誤面單與正常面單走同一條出口，工控機如同一般面單自行列印（所有模式皆可印出）。
- 掃描列印 / 自動印單的錯誤面單印表機來源不一致：正常面單用前端印表機設定（`printerMap`），錯誤面單卻用後端 `dispatch_provider` 設定或系統預設印表機，兩者未同步時造成「正常面單印得出、錯誤面單卻印不出或印錯印表機」。修正為錯誤面單一律用該物流商在「印表機設定」的同一台印表機印出。

### 調整
- 錯誤面單列印失敗（找不到印表機 / 列印失敗 / 暫存失敗）時，桌面 App 顯示提示（先前僅後端記錄、操作員無感）。

### 工控機整合注意
- `GET /api/parcel` 對雲端業務錯誤（門市關轉 / 未確認 / 找不到 / 非代寄 / 非轉寄 / 狀態異常等）改回 **HTTP 200**（不再是 502），回應 `data.is_error_label = true` 並帶 `label_path`（錯誤面單）、`error_code`、`message`。工控機需依 `is_error_label` 旗標判斷，並把 `label_path` 當一般面單印出；錯誤面單無 `response_id`，不需 `POST /api/report`。僅 401 未登入仍回 401、暫存失敗才退回 502。詳見 `docs/local-http-api.md`。

## v0.5.3

### 新增
- 錯誤面單自動列印：工控機查詢失敗時自動產生 100×100mm 錯誤面單並送印，支援 7 種狀態圖案（門市關轉 ⊗、訂單未確認 ?、找不到包裹 🔍、非代寄 ⊘/、非轉寄 ⊘\、包裹異常 □!、一般錯誤 ⚠）；三條路徑均支援（IPC 工控機 / 掃描列印 / 自動印單）。HTTP 404 回傳 NOT_FOUND 代碼時亦正確觸發。
- 分揀通道：新增「未指派物流預設通道代碼」設定，當包裹物流商未對應到任何分揀通道時，回傳此代碼供工控機分流；若未設定則不回傳 `label_path`（無通道可分揀時一併不回傳面單路徑）。
- DirectPrint 直接列印模式：面單路徑模式新增「本機直接列印」，工控機查詢成功後直接呼叫本機印表機列印，不回傳路徑；需於指派物流設定頁為各物流商指定印表機。
- 工控機查詢效能計時：`parcel_query_log` 新增雲端查詢耗時（`cloud_ms`）、面單處理耗時（`label_ms`）、總耗時（`total_ms`），查詢記錄頁顯示並支援即時刷新。
- 統一事件日誌系統：各模組透過統一介面非同步寫入 `event_log`，支援分類與層級。

### 調整
- 側邊欄：桌面版標題列新增收合/展開按鈕，點擊可切換 icon-only 縮合模式，狀態跨頁保留。
- 指派物流設定頁：DirectPrint 模式下可為各物流商指定本機印表機；編輯與刪除彈出視窗改為 persistent（點遮罩不關閉），並加右上角關閉按鈕。
- 工控機失敗提示音：新增 NOT_PROXY（非代寄）、NOT_FORWARD（非轉寄）提示音分類。
- 錯誤面單：移除 UNAUTHORIZED 類型；各路徑觸發條件細化（掃描列印排除 SHIPPING-REPEAT / NO-PRINT-TYPE / LABEL-UNUSUAL 等；自動印單排除 ABNORMAL-PACKAGE / WRAPPER-ERROR）。

## v0.5.2

### 修正
- 所有記錄的時間顯示比台灣時間慢 8 小時：SQLite `datetime('now')` 預設回傳 UTC，修正為 `datetime('now','localtime')`；並加入 migration 將現有記錄補正 +8 小時。受影響表：`event_log`、`report_queue`、`parcel_query_log`、`scan_history`、`sticker_history`、`sort_channels`、`cache_meta`、`dispatch_provider`、`settings`、`printer_profile`。

### 調整
- 面單預產:批次預產並發數從 5 提高到 20,大批量（萬筆級）的預產速度提升約 4 倍。

## v0.5.1

### 修正
- 面單預產:清關日期 / 轉寄出貨等大量批次(6000~12000 筆)會把畫面渲染到卡死、需強制關閉重開 —— 改用瀏覽器可視渲染(`content-visibility`)+ 圖片延遲載入(lazy)+ 計數器統計,只實際渲染/載入畫面內的縮圖,上萬筆也順暢;縮圖仍可完整捲動瀏覽。

### 調整
- 面單預產:輸入切換改為書籤式分頁(訂單編號 / 清關袋號 / 清關日期 / 轉寄出貨),選中頁籤綠底高亮、換頁保留選擇。
- 面單預產:清關袋號改為可一次填多組(逐袋反查整袋訂單,合併去重後一起預產)。
- 面單預產:日期改用 Vuetify 日曆選擇器(中文月份、配色正常);進度列加上成功筆數;欄位標題改為獨立於輸入框上方。

## v0.5.0

### 新增
- 件數核對頁(主功能群):工控機跑單後自動呈現最近 3 袋,顯示整袋訂單清單、各件列印時間,與「件數 / 已印 / 未印」核對;事件驅動即時更新、切頁保留、可一鍵清除。
- 工控機請求失敗聲音提示:`GET /api/parcel` 失敗時依雲端回傳狀態碼(門市關轉 / 訂單未確認 / 找不到 / 未登入 / 一般失敗)播放對應提示音 + toast,現場不必盯著畫面。
- 面單預產:輸入模式擴充為四種 —— 訂單編號 / 清關袋號 / 清關日期 / 轉寄出貨。清關袋號可一次填多組反查整袋訂單;清關日期、轉寄出貨依日期批次反查當日整批訂單,全部收斂到同一套並發預產。模式選擇換頁保留。
- 儀表板:中介服務卡下方新增「工控機連線位址」,列出本機各網卡 LAN IP(`ip:port`),部署時工控機直接照著連(取代只顯示 `0.0.0.0`)。

### 調整
- 「分揀通道」移到主功能群。
- 「Middleware」中文化為「中介服務」(含設定頁提示)。
- 面單預產進度列加上成功筆數;日期選擇改用 Vuetify 日曆元件(中文月份、配色正常)。

> 部分功能(清關袋號/清關日期/轉寄出貨反查、失敗聲音的狀態碼分類、連線位址用的集包單號)需搭配雲端 API 對應端點一併部署才會生效。

## v0.4.2

### 修正
- 自動印單:補上 ABNORMAL-PACKAGE(訂單異常袋號)處理 — 清空包裹條碼並重新聚焦,讓操作員直接重刷袋號。

### 新增 / 調整
- 自動印單:送出 API 前同步清空輸入欄並聚焦,雲端取圖/列印往返期間即可連刷下一筆;回應後不再清空,避免清掉等待期間已刷入的下一筆。
- 自動印單:新增列印序列化佇列 — 連刷照順序、但雲端取圖+原生列印逐筆排隊執行,避免並發撞印表機 spooler、觸發雲端節流、或反查清單彼此覆蓋狀態。
- 自動印單:標題列新增「列印中 N」即時佇列指示(含 spinner),連刷後可看到尚未消化的筆數。
- 自動印單:左欄(列印面單卡 + 入口模式開關)套用 sticky,右側清單長捲時操作區固定不動。
- 列印前必填驗證抽成共用流程,驗證失敗不清欄位、不排隊,兩個掃描入口行為一致。
- 掃描列印:`left-sticky` 區塊內容排版重縮排對齊。

## v0.4.1

- 人員下拉記憶(操作 / 貼單 / 貼標共用歷史)。
- 中文殘片 / 左右等寬顯示修正。

## v0.4.0

- 自動更新基礎建設(Tauri updater + `latest.json`)。
- 印單統計大幅擴充。
- 自動印單反查模式;反查無袋號改為單筆顯示、不累加。
- Linux 三 distro 補簽章 env;自動印單未刷邏輯 / 反查直印修正。
