# 變更紀錄

本檔記錄各版本的修改與調整內容,標題版本號對應 GitHub Release tag。
`release.yml` 會依 tag 自動抽取對應段落,寫進 Release 說明與 `latest.json`(in-app 更新提示)。

## v0.5.8

### 新增
- **印單統計新增「依分揀通道」**:依工控機分揀機出單的分揀通道代碼,統計區間內各通道去重後的印單數。為此 `print_event` 新增 `channel_code` 欄位(僅 `ipc` 工控機來源帶值),升級後開始累積。
- **面單預產自動排程**:新增常駐背景排程,每天到指定時間點(可自訂多個)自動反查當日整批訂單並預下載面單到本機快取——免開預產頁、免人操作(headless)。可選清關日期 / 轉寄出貨來源;每日去重(多時段重跑只抓新出現的單);預設關閉,於「面單預產」頁的「自動排程」設定。

### 調整
- **件數核對頁改版**:
  - 卡片改 Masonry 瀑布流(最新在左上、由左到右填滿,不等高也向上緊貼補空隙,重排平滑滑動)。
  - 訂單明細改為可收合(預設收合,點「展開」開啟)。
  - 列表精簡:移除序號欄、簡碼移至最前、列印時間只顯示 `HH:MM:SS`、欄寬縮窄;移除與下方統計重複的「未印 N 件」徽章。
  - **保留邏輯調整**:有未印件的袋持續保留、永不淘汰(現場可隨時回補列印);已完成的袋只保留最新一袋(原為固定最近 3 袋)。
  - 移除「整袋清單載入失敗」狀態:散單 / 未登入 / 雲端查詢失敗時不再建立錯誤卡(當下那件本身已正常列印,僅次要的整袋交叉核對取不到清單),失敗改記事件記錄。

### 開發
- 修正 `tauri.conf.json` 的 `devUrl`(`1420` → `11420`)對齊 `vite.config.js` 的 dev port,`npm run tauri:dev` 才連得上前端(僅影響開發,不影響打包產物)。

## v0.5.7

### 修正
- 「請求記錄」頁輸入關鍵字必出現 `資料庫錯誤: (code: 20) datatype mismatch`：SQL 以 `?1` 編號參數混用匿名 `?`（LIMIT/OFFSET），sqlx 位置綁定與 SQLite 參數編號規則對不上，LIKE 字串被綁進 LIMIT。改為全匿名 `?` 重複綁定，並新增回歸測試（`src-tauri/tests/parcel_query_log_keyword.rs`）防再犯。

### 新增
- 「請求記錄」進階查詢新增**耗時篩選**：可選欄位（任一 / 雲端 / 圖案 / 總花）搭配毫秒門檻，列出超過執行時間的查詢，便於定位慢請求；可與關鍵字疊加。

### 調整
- 錯誤面單版面：移除整張外框線；新增列印日期 `MM/DD`（非粗體，位於分隔線與圖示之間置中，DejaVu Sans Regular 編譯期內嵌、無須部署字型檔）；`UNCONFIRMED` 問號圖示上移對齊視覺中心。

## v0.5.6

### 修正
- 錯誤面單回應 `channel_code` 一律為 `null`，分揀機無格口可分揀導致不列印：現改為照**正常面單同一套流程**解析分揀通道——雲端錯誤帶出 `shipping_provider` 時（門市關轉 / 未確認 / 狀態異常 / 非代寄 / 非轉寄 / 出單失敗），走指派通道 round-robin 與 `print_profile` 查詢；查不到物流商時（查無訂單 / 雲端連線失敗）統一退回「未指派通道代碼」。只要設定頁有設未指派通道，**所有錯誤面單都保證有 `channel_code`**。
- `direct_print` 模式錯誤面單印表機選擇：可判斷物流商時優先用該物流商在「指派物流」頁設定的印表機（原為無序取任一台已設定印表機，可能印到別站）。

- 錯誤面單回報 `POST /api/report` 會失敗：錯誤查詢先前不寫入查詢記錄且 `response_id` 為 `null`，工控機照正常流程回報時反查不到記錄而收到 422。現錯誤查詢**同步寫入查詢記錄**（`response_id` 用本地負數，與雲端正數 ID 區隔），回報可正常配對回 200（只記錄本機、不推雲端 webhook）；「查詢記錄」頁同時也看得到錯誤查詢（含耗時欄位），不再是診斷盲區。
- 「事件記錄」與「佇列歷史」頁的關鍵字搜尋無作用：過濾邏輯只實作在瀏覽器預覽的 mock 分支，實際 App 內關鍵字從未送到後端。現後端 `event_log_list`（對 `message` / `action`）與 `queue_list`（對 `tracking_no` / `sort_channel` / `job_sticker`）支援 LIKE 模糊查詢，前端同步帶入關鍵字。

### 工控機整合注意
- 錯誤面單（`is_error_label: true`）回應的 `channel_code` / `print_profile` **不再一律為 `null`**，且帶**負數 `response_id`**：工控機把錯誤包裹當一般面單跑完整流程（分揀進 `channel_code` 通道 → 列印 `label_path` → `POST /api/report` 回報）即可，**端上零特殊處理**。詳見 `docs/local-http-api.md`。
- 雲端 API（cix3752iWeb）需同步部署：`OrderPrintController::failResp` 在查得到訂單的業務錯誤帶出 `shipping_provider`。雲端未部署時，所有錯誤面單退回未指派通道代碼（不影響運作，但無法分到物流商對應通道）。

## v0.5.5

### 新增
- **設備異常語音廣播 API（`POST /api/device-alert`）**：工控機回報設備異常（分揀機台卡包裹、USB 接口異常斷線、掃描器 / 印表機故障等）時，桌面 App 以**中文 + 越南語雙語語音**廣播喊話現場人員，並顯示 toast 提示。語音採**預錄音檔內嵌**（中文 `HsiaoChen`、越南語 `HoaiMy` neural 音色），每台機器音色一致、發音標準、**離線可用、越南語免在 Windows 端裝語音包**；僅未錄音的自訂 `type` 才退回系統 TTS。支援 `repeat` 控制廣播次數（預設 1、上限 3）、`message` 補充細節（顯示於 toast）。內建分類碼：`PARCEL_JAM` / `USB_DISCONNECT` / `SCANNER_ERROR` / `PRINTER_ERROR` / `ERROR`。
- **面單預產去重快取**：面單預產頁在同一「快取日」（以 04:00 為界，對齊面單快取清理週期）內，已成功預產過的訂單編號不再重打雲端 `cloudFetchLabel`，避免大量批次重複請求、降低伺服器負擔。統計列新增「略過」筆數顯示，縮圖以綠色勾選圖示標示被略過的訂單。

### 工控機整合注意
- 新增 `POST /api/device-alert` 端點：**立即回 200**（不讓工控機等），語音廣播由桌面 App 背景處理。`type` 自動轉大寫（大小寫皆可傳），省略時當作 `ERROR`；`repeat` 自動限制在 1～3。此端點與查件 / 回報流程無關，可獨立呼叫。詳見 `docs/device-alert-api.md`（亦提供 `.docx` 給整合廠商）。

## v0.5.4

### 修正
- 錯誤面單在 `http` / `local` / `share` 面單路徑模式下印不出來：根因為錯誤面單只走中介機本機印表機（`find_any_printer`），但這些模式下印表機在工控機端、中介機無印表機，導致錯誤面單被靜默丟棄（先前僅 `direct_print` 模式可用）。修正後工控機 `GET /api/parcel` 遇雲端業務錯誤改回 **HTTP 200** + `is_error_label` 旗標與 `label_path`，錯誤面單與正常面單走同一條出口，工控機如同一般面單自行列印（所有模式皆可印出）。
- 掃描列印 / 自動印單的錯誤面單印表機來源不一致：正常面單用前端印表機設定（`printerMap`），錯誤面單卻用後端 `dispatch_provider` 設定或系統預設印表機，兩者未同步時造成「正常面單印得出、錯誤面單卻印不出或印錯印表機」。修正為錯誤面單一律用該物流商在「印表機設定」的同一台印表機印出。

### 調整
- 錯誤面單列印失敗（找不到印表機 / 列印失敗 / 暫存失敗）時，桌面 App 顯示提示（先前僅後端記錄、操作員無感）。

### 工控機整合注意
- `GET /api/parcel` 對雲端業務錯誤（門市關轉 / 未確認 / 找不到 / 非代寄 / 非轉寄 / 狀態異常等）改回 **HTTP 200**（不再是 502），回應 `data.is_error_label = true` 並帶 `label_path`（錯誤面單）、`error_code`、`message`。工控機需依 `is_error_label` 旗標判斷，並把 `label_path` 當一般面單印出；錯誤面單無 `response_id`，不需 `POST /api/report`。僅 401 未登入仍回 401、暫存失敗才退回 502。詳見 `docs/local-http-api.md`。

## v0.5.3

### 新增
- 錯誤面單自動列印：工控機查詢失敗時自動產生 100×100mm 錯誤面單並送印，支援 7 種狀態圖案（門市關轉 ⊗、訂單未確認 ?、找不到包裹 🔍、非代寄 ⊘/、非轉寄 ⊘\、包裹異常 □!、一般錯誤 ⚠）；三條路徑均支援（IPC 工控機 / 掃描列印 / 自動印單）。HTTP 404 回傳 NOT_FOUND 代碼時亦正確觸發。
- 分揀通道：新增「未指派物流預設通道代碼」設定，當包裹物流商未對應到任何分揀通道時，回傳此代碼供工控機分流；若未設定則不回傳 `label_path`（無通道可分揀時一併不回傳面單路徑）。
- DirectPrint 直接列印模式：面單路徑模式新增「本機直接列印」，工控機查詢成功後直接呼叫本機印表機列印，不回傳路徑；需於指派物流設定頁為各物流商指定印表機。
- 工控機查詢效能計時：`parcel_query_log` 新增雲端查詢耗時（`cloud_ms`）、面單處理耗時（`label_ms`）、總耗時（`total_ms`），查詢記錄頁顯示並支援即時刷新。
- 統一事件日誌系統：各模組透過統一介面非同步寫入 `event_log`，支援分類與層級。

### 調整
- 側邊欄：桌面版標題列新增收合/展開按鈕，點擊可切換 icon-only 縮合模式，狀態跨頁保留。
- 指派物流設定頁：DirectPrint 模式下可為各物流商指定本機印表機；編輯與刪除彈出視窗改為 persistent（點遮罩不關閉），並加右上角關閉按鈕。
- 工控機失敗提示音：新增 NOT_PROXY（非代寄）、NOT_FORWARD（非轉寄）提示音分類。
- 錯誤面單：移除 UNAUTHORIZED 類型；各路徑觸發條件細化（掃描列印排除 SHIPPING-REPEAT / NO-PRINT-TYPE / LABEL-UNUSUAL 等；自動印單排除 ABNORMAL-PACKAGE / WRAPPER-ERROR）。

## v0.5.2

### 修正
- 所有記錄的時間顯示比台灣時間慢 8 小時：SQLite `datetime('now')` 預設回傳 UTC，修正為 `datetime('now','localtime')`；並加入 migration 將現有記錄補正 +8 小時。受影響表：`event_log`、`report_queue`、`parcel_query_log`、`scan_history`、`sticker_history`、`sort_channels`、`cache_meta`、`dispatch_provider`、`settings`、`printer_profile`。

### 調整
- 面單預產:批次預產並發數從 5 提高到 20,大批量（萬筆級）的預產速度提升約 4 倍。

## v0.5.1

### 修正
- 面單預產:清關日期 / 轉寄出貨等大量批次(6000~12000 筆)會把畫面渲染到卡死、需強制關閉重開 —— 改用瀏覽器可視渲染(`content-visibility`)+ 圖片延遲載入(lazy)+ 計數器統計,只實際渲染/載入畫面內的縮圖,上萬筆也順暢;縮圖仍可完整捲動瀏覽。

### 調整
- 面單預產:輸入切換改為書籤式分頁(訂單編號 / 清關袋號 / 清關日期 / 轉寄出貨),選中頁籤綠底高亮、換頁保留選擇。
- 面單預產:清關袋號改為可一次填多組(逐袋反查整袋訂單,合併去重後一起預產)。
- 面單預產:日期改用 Vuetify 日曆選擇器(中文月份、配色正常);進度列加上成功筆數;欄位標題改為獨立於輸入框上方。

## v0.5.0

### 新增
- 件數核對頁(主功能群):工控機跑單後自動呈現最近 3 袋,顯示整袋訂單清單、各件列印時間,與「件數 / 已印 / 未印」核對;事件驅動即時更新、切頁保留、可一鍵清除。
- 工控機請求失敗聲音提示:`GET /api/parcel` 失敗時依雲端回傳狀態碼(門市關轉 / 訂單未確認 / 找不到 / 未登入 / 一般失敗)播放對應提示音 + toast,現場不必盯著畫面。
- 面單預產:輸入模式擴充為四種 —— 訂單編號 / 清關袋號 / 清關日期 / 轉寄出貨。清關袋號可一次填多組反查整袋訂單;清關日期、轉寄出貨依日期批次反查當日整批訂單,全部收斂到同一套並發預產。模式選擇換頁保留。
- 儀表板:中介服務卡下方新增「工控機連線位址」,列出本機各網卡 LAN IP(`ip:port`),部署時工控機直接照著連(取代只顯示 `0.0.0.0`)。

### 調整
- 「分揀通道」移到主功能群。
- 「Middleware」中文化為「中介服務」(含設定頁提示)。
- 面單預產進度列加上成功筆數;日期選擇改用 Vuetify 日曆元件(中文月份、配色正常)。

> 部分功能(清關袋號/清關日期/轉寄出貨反查、失敗聲音的狀態碼分類、連線位址用的集包單號)需搭配雲端 API 對應端點一併部署才會生效。

## v0.4.2

### 修正
- 自動印單:補上 ABNORMAL-PACKAGE(訂單異常袋號)處理 — 清空包裹條碼並重新聚焦,讓操作員直接重刷袋號。

### 新增 / 調整
- 自動印單:送出 API 前同步清空輸入欄並聚焦,雲端取圖/列印往返期間即可連刷下一筆;回應後不再清空,避免清掉等待期間已刷入的下一筆。
- 自動印單:新增列印序列化佇列 — 連刷照順序、但雲端取圖+原生列印逐筆排隊執行,避免並發撞印表機 spooler、觸發雲端節流、或反查清單彼此覆蓋狀態。
- 自動印單:標題列新增「列印中 N」即時佇列指示(含 spinner),連刷後可看到尚未消化的筆數。
- 自動印單:左欄(列印面單卡 + 入口模式開關)套用 sticky,右側清單長捲時操作區固定不動。
- 列印前必填驗證抽成共用流程,驗證失敗不清欄位、不排隊,兩個掃描入口行為一致。
- 掃描列印:`left-sticky` 區塊內容排版重縮排對齊。

## v0.4.1

- 人員下拉記憶(操作 / 貼單 / 貼標共用歷史)。
- 中文殘片 / 左右等寬顯示修正。

## v0.4.0

- 自動更新基礎建設(Tauri updater + `latest.json`)。
- 印單統計大幅擴充。
- 自動印單反查模式;反查無袋號改為單筆顯示、不累加。
- Linux 三 distro 補簽章 env;自動印單未刷邏輯 / 反查直印修正。
