# === Node ===
node_modules/
dist/
.vite/
yarn.lock
package-lock.json

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Editor / IDE ===
.vscode/
.idea/
*.swp
*.swo

# === macOS ===
.DS_Store

# === Office 暫存 / 鎖檔(Word / Pandoc) ===
~$*
.~*

# === Rust / Tauri 產物 ===
/src-tauri/target/
/src-tauri/gen/

# === 本地資料(執行時生成) ===
/data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# === 環境變數 ===
.env
.env.local

# === 本地備份(搬移過的檔案、reference 截圖等) ===
backups/

# === AI 工具工作目錄 ===
.claude/
.serena/

# === 測試產物(tests/docker-ubuntu-build.sh build 出的 tarball,40MB+ 不入 git)===
/tests/tarballs/

# === 程式碼圖譜索引產物(本地分析用,不入 git)===
# .gitnexus 已自帶內層 .gitignore;graphify-out 為 graphify 產出(graph.html/json 約 3MB)
graphify-out/
!package-lock.json
# ^ lock 檔入版控以啟用 Dependabot 漏洞掃描
